{
  "name": "ink-text-autocomplete",
  "version": "0.1.4",
  "description": "An Ink component built with Bun and TypeScript",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/ahwuoc/ink-text-autocomplete.git"
  },
  "type": "module",
  "private": false,
  "scripts": {
    "clean": "rm -rf dist",
    "dev": "bun --hot run example/index.tsx",
    "build": "bun build src/index.tsx --outdir ./dist --splitting --format esm --target node && tsc --emitDeclarationOnly --outDir dist"
  },
  "peerDependencies": {
    "react": "^19.1.0",
    "ink": "^6.0.0",
    "ink-text-input": "^6.0.0"
  }
}
